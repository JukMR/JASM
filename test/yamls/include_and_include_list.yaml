---
patterns:
  - call
  - $any:
      include_list:
        inst:
          - jmp
          - endbr64
          - nopw
        operands:
          include_list:
            ops:
              - rx
              - rd
              - rax

          exclude_list:

      exclude_list:
        inst:
          - not
          - mov
          - call
          - add
        operands:

      times:
        min: 1
        max: 1
  - endbr64
